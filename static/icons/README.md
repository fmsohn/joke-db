# PWA Icons

Add these icon files for the Stagetime PWA:

- **AppIcon.png** – 192×192 (or larger), purpose: any
- **AppIconSafe.png** – 192×192 (or larger), purpose: maskable (keep important content inside a safe circle; ~80% of width/height)

Both are referenced by `manifest.json` and cached by the service worker.
